_group.createIfNotExists(
    _val.map()
        .set("name", "Pessoa")
        .set("code", "people")
)

_group.createIfNotExists(
    _val.map()
        .set("name", "Administrador")
        .set("code", "admin")
)