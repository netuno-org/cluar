import React from "react";

import { Form } from "antd";

const ListEditor = ({ sectionData }) => {
  return (
    <div>
      <Form.Item label="items"></Form.Item>
    </div>
  );
};

export default ListEditor;
